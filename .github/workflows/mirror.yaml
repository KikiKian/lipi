name: Mirror to Codeberg
on:
  push:
    branches: [main]
jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: |
          git remote add codeberg https://oauth2:${{ secrets.9c52414c18923b956f70c5767339fd71439d21d0}}@codeberg.org/i-am-kiki/lipi.git
          git push codeberg main --force
